# Build artifacts
/nudibranch
/fetch-inat
*.exe

# Assembled static site (built by `make site` / the Pages workflow)
/_site/

# NOAA tide response cache
/tidecache/

# Local, machine-specific Claude settings (keep launch.json shared)
/.claude/settings.local.json
